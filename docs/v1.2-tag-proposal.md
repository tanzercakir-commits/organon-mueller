# v1.2.0 Version-Tag PROPOSAL

**Status: PROPOSAL — not applied.** This document proposes a release
tag for `organon-mueller`. It does **not** create one. Tagging a
release (and publishing the GitHub Release, which auto-publishes to
PyPI via the trusted-publishing workflow) is a user decision. No
`git tag` command was run in producing this document.

## Proposal

Apply an annotated tag **`v1.2.0`** to `main` at the FROZEN-7 closure
commit (the L6 consolidation commit carrying this file). The release
adds the complete Lorentz face to the engine.

## What v1.2.0 contains (release-notes material)

Everything since v1.1.1, i.e. the FROZEN-7 series L0–L6 — built from a
collaborator's written work order (Σ^μ basis, Z = α_μΣ^μ, Λ = ZZ*,
three tasks):

- **`organon_mueller.lorentz.core`** — Σ/Σ̄ bases (identical to the
  engine's polarization-face Z-matrix basis: the bridge theorem),
  Z/Z̄/guarded inverse/Λ, boost and rotation parametrizations with
  exact textbook anchors, metric-factor theorem ΛᵀgΛ = q q̄ g.
- **`lorentz.identities`** — the work order's five Task-1 identities,
  each locked as the q = 1 corollary of a guard-free theorem (row-index
  Λ^μ_ν forced, column form pinned FALSE; conjugation lemma
  Z(α)* = Z(α*)ᵀ); the five Task-2 Σ̄ identities DISCOVERED and locked
  (forced coefficient matrix C = gΛᵀg = Λ(Z̄); bonus theorems
  Λ(Z̄) = gΛᵀg and CΛ = ΛC = q q̄ I).
- **`lorentz.terms`** — 8-letter term language; registry-blind
  numeric classifier + generic symbolic certifier; SELF-RECOVERY GATE
  passed 10/10 (v2 A5 tradition).
- **`lorentz.discovery`** — the Task-3 sweep: all 128 sandwiches
  decided exactly (40 identities / 88 symbolic negative certificates);
  30 new statements beyond the work order's ten; Λ-family closure
  (8 members); pair-product coefficient structure (proven
  case-by-case; universal formula deliberately not claimed);
  falsified-conjecture pins; committed reproducible sweep table
  (`reports/sweep-lorentz-01.json`).
- Tests 368 → **399** (bare suite green; CI Linux ×3 + Windows).
  No UI/parse-surface changes: α never enters as text anywhere.
- Process addition: docs-guard-last-run rule after the L3/L4
  count-guard incident (owned in the L4 commit message).

## Suggested annotated-tag message (draft — edit freely)

```
v1.2.0 — organon-mueller: the Lorentz face (FROZEN-7 complete)

Adds organon_mueller.lorentz: the collaborator work order's Σ^μ/Z/Λ
algebra as a first-class face of the engine. Tasks 1–2 verified and
completed (guard-free theorem forms, forced conventions, bonus
theorems); Task 3 answered on the declared 128-sandwich space —
completely decided: 40 identities (30 new) / 88 certified
non-identities, self-recovery-gated discovery machinery, reproducible
sweep table. 399 tests green; CI Linux ×3 + Windows. Experimental
research software, MIT; no novelty-to-literature claims (human
judgement reserved).
```

## The commands — for the user to run, if and when they choose

```bash
git tag -a v1.2.0 -m "v1.2.0 — organon-mueller: the Lorentz face (FROZEN-7 complete)"
git push origin v1.2.0
# then publish the GitHub Release from the tag — publish.yml
# auto-publishes to PyPI (trusted publishing, no tokens).
```

## Boundary

Tagging and Release publication remain the user's actions. Also still
gated and untaken: egglog upstream issue, MCP hosting, exposure beyond
localhost. Collaborator report #2 is delivered through the user
(send gate: user), and is not part of the repository.
